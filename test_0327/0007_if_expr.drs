fn f() {
    (
        // 标准格式
        , if. x bigger then (y) {
              123
          } else if. x smaller than (y) {
              223
          }
        , if (x bigger then (y)) {
              123
          } else if (x smaller than (y)) {
              223
          }
        , if {
            , x bigger than (y) {
                123
              }
            , x smaller than (y) {
                223
              }
            , else {
                323
              }
          }

        , if x bigger then (y) {
              123
          }
        , if x bigger then (y) {
              123
          } else if x smaller then (y) {
              223
          }
        , if (x bigger then (y)) {
              123
          } else {
              623
          }
        , if. x bigger then (y) {
              123
          } else if x smaller then (y) {
              223
          } else {
              323
          }
        , if{ , x bigger then (y) {
                  123
                }
              , x smaller the (y) {
                  223
                }
          }
        , if{ , x bigger then (y) {
                  123
                }
              , x smaller the (y) {
                  223
                }
              , else {
                  423
                }
          }
        , if {  x bigger then (y) {
                  123
                }
              , x smaller the (y) {
                  223
                }
          } else {
              523
          }
        /* , 123 .when x bigger than (y)           */
        /* , 123 .when x bigger than (y) .else 223 */
    )
}
